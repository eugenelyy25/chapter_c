#include <stdio.h>
#include <math.h>
#include <assert.h>

void test(void);
int circle(int x);

int main(void)
{
    int x;
    
    printf("Input a radian: ");
    scanf("%lf", &x);
    if (x >= 0 && x <= 2*PI){
        int circle(int x);
        printf("%d\n",x);
    }
    else
    {
        printf("Invalid Input");
    }
    return 0;
}

int circle(int x)
{
    // Calculate sin
}

void test(int x)
{
    assert ()
}